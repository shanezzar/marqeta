//
// AndroidPushTokenizeRequestData.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation


/** Contains details about a card tokenization push request. */

public struct AndroidPushTokenizeRequestData: Codable {

    /** Name of the card as displayed in the digital wallet, typically showing the card brand and last four digits of the primary account number (PAN). &#x60;Visa 5678&#x60;, for example. */
    public var displayName: String?
    /** Last four digits of the primary account number of the physical or virtual card. */
    public var lastDigits: String?
    /** Specifies the card network of the physical or virtual card. */
    public var network: String?
    /** Encrypted data field created by the issuer and passed to Google Wallet during the push provisioning process. */
    public var opaquePaymentCard: String?
    /** Specifies the network that provides the digital wallet token service. */
    public var tokenServiceProvider: String?
    public var userAddress: AndroidPushTokenRequestAddress?

    public init(displayName: String? = nil, lastDigits: String? = nil, network: String? = nil, opaquePaymentCard: String? = nil, tokenServiceProvider: String? = nil, userAddress: AndroidPushTokenRequestAddress? = nil) {
        self.displayName = displayName
        self.lastDigits = lastDigits
        self.network = network
        self.opaquePaymentCard = opaquePaymentCard
        self.tokenServiceProvider = tokenServiceProvider
        self.userAddress = userAddress
    }

    public enum CodingKeys: String, CodingKey { 
        case displayName = "display_name"
        case lastDigits = "last_digits"
        case network
        case opaquePaymentCard = "opaque_payment_card"
        case tokenServiceProvider = "token_service_provider"
        case userAddress = "user_address"
    }

}
